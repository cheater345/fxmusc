org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Use build-tools 34.0.0 aapt2, which is compatible with this ARM environment
# (newer bundled aapt2 binaries crash with "Illegal instruction" here).
android.aapt2FromMavenOverride=/opt/android-sdk/build-tools/34.0.0/aapt2
# Apply init script to patch flutter_inappwebview_android proguard-android.txt issue
org.gradle.init.script=android/inappwebview-patch.gradle
